<UserControl
    x:Class="MaaWpfGui.Views.UserControl.TaskQueue.StoreShoppingSettingsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:controls="clr-namespace:MaaWpfGui.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:taskQueue_vms="clr-namespace:MaaWpfGui.ViewModels.UserControl.TaskQueue"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type taskQueue_vms:StoreShoppingSettingsUserControlModel}}"
    d:DesignWidth="220"
    s:View.ActionTarget="{Binding}"
    mc:Ignorable="d">
    <StackPanel Visibility="{c:Binding !EnableAdvancedSettings, Source={x:Static ui_vms:TaskQueueViewModel.TaskSettingVisibilities}}">
        <StackPanel Margin="0,10">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="{DynamicResource MiniGameNameGreenTicketStore}" IsChecked="{Binding BuyGreenTicket}" />
                <controls:TooltipBlock TooltipText="{DynamicResource MiniGameNameGreenTicketStoreTip}" />
            </StackPanel>
            <controls:TextBlock
                Margin="0,5,0,0"
                HorizontalAlignment="Left"
                TextWrapping="Wrap"
                Visibility="{c:Binding 'GreenTicketLastSuccessText.Length > 0'}">
                <Run Text="{DynamicResource StoreShoppingLastSuccess}" />
                <Run Text=": " />
                <Run Text="{Binding GreenTicketLastSuccessText, Mode=OneWay}" />
            </controls:TextBlock>
        </StackPanel>
        <StackPanel Margin="0,10">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="{DynamicResource MiniGameNameYellowTicketStore}" IsChecked="{Binding BuyYellowTicket}" />
                <controls:TooltipBlock TooltipText="{DynamicResource MiniGameNameYellowTicketStoreTip}" />
            </StackPanel>
            <controls:TextBlock
                Margin="0,5,0,0"
                HorizontalAlignment="Left"
                TextWrapping="Wrap"
                Visibility="{c:Binding 'YellowTicketLastSuccessText.Length > 0'}">
                <Run Text="{DynamicResource StoreShoppingLastSuccess}" />
                <Run Text=": " />
                <Run Text="{Binding YellowTicketLastSuccessText, Mode=OneWay}" />
            </controls:TextBlock>
        </StackPanel>
        <StackPanel Margin="0,10">
            <hc:ComboBox
                VerticalContentAlignment="Center"
                hc:InfoElement.Title="{DynamicResource UserDataUpdateTriggerInterval}"
                hc:TitleElement.HorizontalAlignment="Left"
                DisplayMemberPath="Display"
                ItemsSource="{Binding TriggerIntervalList}"
                SelectedValue="{Binding TriggerInterval}"
                SelectedValuePath="Value" />
        </StackPanel>
    </StackPanel>
</UserControl>
